import React from 'react';
import {View, Text} from 'react-native';

import styles from './index.styles';

const ModelCoppy: React.FC = () => {
  return (
    <View style={styles.wrapper}>
      <Text> ModelCoppy </Text>
    </View>
  );
};

export default ModelCoppy;
